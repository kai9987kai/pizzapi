# Everything needed to work on pizzapy, runtime and tests together.
# Installing the library itself only pulls in requests and xmltodict
# (see install_requires in setup.py).
requests
xmltodict

# Tests only.
mock
pyhamcrest
pytest
